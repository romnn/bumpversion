# yaml-language-server: $schema=https://raw.githubusercontent.com/SchemaStore/schemastore/refs/heads/master/src/schemas/json/github-workflow.json
name: pages
on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "crates/**"
      - "scripts/terminals.sh"
      - "mise.toml"
      - "mise.lock"
      - "taskfile.yaml"
      - ".github/workflows/pages.yaml"
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  MISE_AUTO_INSTALL: "0"
  MISE_GITHUB_TOKEN: ${{ github.token }}

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
        with:
          version: 2026.7.16
          install: false
          cache: true
          env: false
      - run: >-
          mise install go "asdf:NeoHsu/asdf-hugo" task
          "github:buildkite/terminal-to-html"
      # `task docs:build` regenerates the terminal snippets from the real binary
      # before Hugo runs, so a published page can never show output the current
      # code and the committed docs/examples/ no longer produce.
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      # scripts/terminals.sh runs bumpversion against throwaway git repos, and its
      # commit step needs an author even under --dry-run.
      - name: Configure git author
        run: |
          git config --global user.email "docs@example.com"
          git config --global user.name "docs"
      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v6
      - name: Build docs
        run: task docs:build -- --baseURL "${{ steps.pages.outputs.base_url }}/"
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/public

  deploy:
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

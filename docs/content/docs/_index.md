---
title: Documentation
bookToc: false
bookFlatSection: false
---

# Documentation

`bumpversion` rewrites every version string in a project from a single config file, then commits and tags the result. It is a Rust rewrite of `bump-my-version` and reads the same configuration files, so an existing project usually needs no changes at all.

## Start here

- **[Introduction]({{< relref "introduction.md" >}})** — what a bump actually does, step by step.
- **[Installation]({{< relref "installation.md" >}})** — install the `bumpversion` binary.
- **[Quick start]({{< relref "quick-start.md" >}})** — your first config file and your first bump.

## Go deeper

- **[Configuration]({{< relref "configuration/_index.md" >}})** — config file formats, the files to rewrite, the version scheme, VCS behavior, hooks, and a full key reference.
- **[Commands]({{< relref "commands/_index.md" >}})** — `bump`, `show`, `show-bump`, every flag, and the exit codes.
- **[Continuous integration]({{< relref "ci.md" >}})** — running a release from GitHub Actions.

> [!NOTE]
> Every terminal transcript on this site is generated by running the real binary against the example projects in [`docs/examples/`](https://github.com/romnn/bumpversion/tree/main/docs/examples), and the config files shown above them are those same files. They are regenerated on every docs build, so an example that stopped working cannot quietly stay on the page.
